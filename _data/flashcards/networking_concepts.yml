title: "Networking Concepts"
description: "Review key networking concepts: architectures, protocols, HTTP, and the TCP/IP stack."
cards:
  - id: 1
    question: "What are the two roles in a **client-server architecture**, and who initiates the connection?"
    answer: "The **client** consumes resources and always **initiates** the connection. The **server** provides resources and passively listens for incoming requests. Multiple clients can connect to the same server simultaneously."

  - id: 2
    question: "How does a **peer-to-peer (P2P)** architecture differ from client-server?"
    answer: "In P2P, there is **no central server**. Every node is **equally privileged** and acts as both a supplier and consumer of resources. It is **decentralized**, so there is no single point of failure — but if a peer goes offline, its unique resources become unavailable."

  - id: 3
    question: "What is a **hybrid architecture**? Give a real-world example."
    answer: "A hybrid combines client-server and P2P. **Zoom** uses hybrid: for 1-on-1 calls it attempts P2P for lower latency, but falls back to client-server if P2P fails or when there are more than 2 participants (to avoid each peer sending their stream N times)."

  - id: 4
    question: "Explain the difference between **throughput** and **latency**."
    answer: "**Throughput** = volume of requests processed per unit time (e.g., 100 req/sec). **Latency** = time for a single request to complete (e.g., 10ms). They are *not* always correlated: adding more servers increases throughput but doesn't reduce per-request latency. Caching reduces latency."

  - id: 5
    question: "Name the four layers of the **TCP/IP stack** from bottom to top and what each layer does."
    answer: "1. **Link Layer** (Ethernet, Wi-Fi) — physical transmission over local hardware. 2. **Internet Layer** (IPv4, IPv6) — addressing and routing between networks. 3. **Transport Layer** (TCP, UDP) — end-to-end communication between applications. 4. **Application Layer** (HTTP, HTTPS, DNS) — interface for apps to access network services."

  - id: 6
    question: "What is **encapsulation** (package wrapping) in the TCP/IP stack?"
    answer: "Each layer wraps the higher-layer message as its **payload** and adds its own **header** — like Russian nesting dolls. An HTTP message gets wrapped in a TCP header, which gets wrapped in an IP header, which gets wrapped in an Ethernet header. Each header contains layer-specific metadata."

  - id: 7
    question: "What is the **TCP three-way handshake** and why is it needed?"
    answer: "**SYN** → **SYN-ACK** → **ACK**. The client sends SYN ('I want to connect'), the server replies SYN-ACK ('OK, I'm ready'), the client confirms with ACK ('let's go'). It ensures both parties are ready to send and receive data before any data is transmitted."

  - id: 8
    question: "How does TCP guarantee reliable delivery during data transfer?"
    answer: "TCP sends data in **ordered** messages with **checksums** for error detection. The receiver sends an **ACK** for each message received correctly. If the sender doesn't receive an ACK within a **timeout**, it **retransmits** the message. This guarantees delivery, order, and integrity — at the cost of additional overhead."

  - id: 9
    question: "What guarantees does **UDP** provide, and what does it sacrifice?"
    answer: "UDP provides **fast, connectionless, lightweight** communication with **no additional delay**. It sacrifices everything else: no guaranteed delivery (messages can be lost), no guaranteed order, and no error detection. It just 'throws' data at the receiver."

  - id: 10
    question: "What does it mean that HTTP is **stateless**?"
    answer: "Each HTTP request is **independent** — the server does not remember any information about previous requests from the same client. Every request must contain all the information the server needs to respond. Web apps use cookies/sessions to maintain state across requests."

  - id: 11
    question: "Name at least three main **HTTP verbs** and what each does."
    answer: "**GET** — retrieve a resource. **POST** — send data to create/update a resource. **PUT** — update an existing resource. **DELETE** — delete a resource. **HEAD** — retrieve only the headers of a resource (not the body)."

  - id: 12
    question: "What is `127.0.0.1` and what is it commonly called?"
    answer: "**Localhost** — it is a special IP address that always refers to *your own machine*. When developing web apps, you run a server on localhost so your browser (also on your machine) can connect to it for testing without needing the internet."

  - id: 13
    question: "What is a **URL** and what are its components?"
    answer: "`{protocol}://{domain}(:{port})(/{resource})`. Example: `http://localhost:8080/users/1`. **Protocol** (http/https), **domain** (the server's address), **port** (which application on the server — optional, defaults to 80/443), **resource path** (which resource to access — optional, defaults to `/`)."

  - id: 14
    question: "What does **HTTPS** add on top of HTTP, and why is it important?"
    answer: "HTTPS adds **SSL/TLS encryption** to HTTP. It protects sensitive data (passwords, personal info) from being intercepted in transit. It has become the **default for all public web pages**, even those without sensitive data, because it also prevents tampering and ensures you're talking to the real server."
