_site
.sass-cache
.bundle
vendor/
node_modules/
playwright-report/
test-results/
.cache/
.playwright-mcp/
.jekyll-metadata
pdfs/
.DS_Store
latex/

.uml_cache/
notes/
js/__pycache__/

# v86 post-boot snapshot — the raw .bin is local-only debugging output; the
# .gz form is what the runtime fetches and ships with the site.
vm/dist/state.bin
